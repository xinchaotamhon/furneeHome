{
  "name": "furneehome",
  "version": "1.0.0",
  "private": true,
  "description": "FurneeHome — AI Interior Room Studio for Students",
  "scripts": {
    "postinstall": "cd server && npm install",
    "start": "node server/src/server.js",
    "build": "cd server && npm install"
  }
}
